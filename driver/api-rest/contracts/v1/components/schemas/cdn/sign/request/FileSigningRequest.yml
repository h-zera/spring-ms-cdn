type: object
title: FileSigningRequest
required:
  - filePath
properties:
  filePath:
    type: string
    description: The path of the file to be signed
    example: /path/to/file.txt
    pattern: ^/([a-zA-Z0-9._-]+/)*([a-zA-Z0-9._-]*[^.^/]$)$
  config:
    $ref: './SigningConfigResource.yml'